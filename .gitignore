# Next.js build output — never commit these
.next/
out/

# Dependencies
node_modules/

# Environment variables — keep secrets out of git
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# TypeScript build info
*.tsbuildinfo
tsconfig.tsbuildinfo
next-env.d.ts

# Claude Code local settings
.claude/

# OS files
.DS_Store
Thumbs.db

# Personal reference docs and screenshots — not needed in repo
my_references/

# Sandbox — local design testing tool, not part of the site
app/sandbox/
